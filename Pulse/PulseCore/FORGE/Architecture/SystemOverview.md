# PulseCore System Overview

PulseCore is a Swift package providing the core library for the Pulse ecosystem. It is designed to be imported by the main application targets and other dependent packages.

Currently, it consists of a single `PulseCore` target containing the core swift source code.
